static SinglyLinkedListNode insertNodeAtTail(SinglyLinkedListNode temp, int data) {
      SinglyLinkedListNode head1 = new SinglyLinkedListNode(data);
      // System.out.println(temp.data);
      if(temp==null)
          return head1;
      SinglyLinkedListNode head = temp;
      while(head.next!=null)
      { head =head.next;
      }
       head.next=head1;
      return temp;
      }
